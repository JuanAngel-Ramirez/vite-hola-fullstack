version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm run install-all
    build:
      commands:
        - npm run build-all
  artifacts:
    baseDirectory: Interfaz/dist
    files:
      - '**/*'
  cache:
    paths:
      - Interfaz/node_modules/**/*